藥檢機器UI程式碼

=======================================================================================
修改部分:

1. 1280x720: 新增白試紙影像。

2. api/camera.py修改: .set(cv2.CAP_PROP_FPS, 5)，FPS降低成5fps(原本為30fps)。

2. JimmyTest/camera.py修改: .set(cv2.CAP_PROP_FPS, 5)，FPS降低成5fps(原本為30fps)。

3. DD.py修改:

自動抓取試紙與線段(creat_cor、MED)(line:619~623、1696) : MED用scipy抓取區域最小值、區域最大值當作線段邊界，改成全自動分類線段。

新增自動抓取相機ID(camera_id)(line:113~118)

解決身分證資料夾重複(line:1680~1681)

對旋轉校正的QRcode影像除雜訊(line:1263~1331) : 降躁讓影像旋轉校正更正確。


。
